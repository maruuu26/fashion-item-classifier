def load_model(checkpoint_path, num_classes=10):
    """
    Rebuild the same model architecture and load state_dict().
    """
    # TODO

def evaluate_on_test(model, test_loader, device):
    """
    Return accuracy and the sklearn confusion_matrix (y_true vs y_pred).
    """
    # TODO
